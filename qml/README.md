# QML Resources

This directory is reserved for future Qt Quick cyberpunk dashboard components.
The first implementation uses custom Qt Widgets so the app can run in a simpler
Qt Widgets + SerialPort environment.
